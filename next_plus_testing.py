import decimal

print(decimal.Decimal(0.0).next_plus())
print(type(decimal.Decimal(0.0).next_plus()))
print(float(decimal.Decimal(0.0).next_plus()))
print(type(float(decimal.Decimal(0.0).next_plus())))